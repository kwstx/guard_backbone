[project]
name = "guard.core"
version = "0.1.0"
description = "Autonomy Core Engine"
dependencies = [
    "pydantic",
    "python-terraform",
    "stripe",
    "cryptography"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
