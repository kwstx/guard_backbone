[project]
name = "gateway_service"
version = "0.1.0"
description = "Gateway service for external agent action orchestration"
dependencies = [
    "guard.core",
    "identity_system",
    "guard.enforcement",
    "simulation_layer",
    "fastapi",
    "uvicorn",
]
