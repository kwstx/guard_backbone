[project]
name = "guard.sdk"
version = "0.1.0"
description = "Autonomy SDK"
dependencies = [
    "guard.core", "identity_system", "guard.enforcement", "economic_autonomy", "a2a_coordination", "guard.scoring", "simulation_layer", "self_improvement_governance"
]
