[project]
name = "guard.sdk"
version = "0.1.0"
description = "Autonomy SDK"
dependencies = [
    "guard.core", "guard.enforcement", "guard.scoring", "guard.logic"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
