[project]
name = "guard.sdk"
version = "0.1.0"
description = "Autonomy SDK"
dependencies = [
    "guard.core", "guard.enforcement", "economic_autonomy", "guard.scoring", "simulation_layer", "self_improvement_governance"
]
