[project]
name = "guard.enforcement"
version = "0.1.0"
description = "guard.enforcement package"
dependencies = [
    "guard.core"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
